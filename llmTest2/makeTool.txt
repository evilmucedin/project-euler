Implement an Ubuntu console script tool, which will open a MD text file (output of an Ollama LLM tool)  given by an argument, extract lines started from a start line with "```cpp" to a stop line with "```" and output it to a new file given by an argument in cpp format for later compilation with g++. Don't forget to convert from MD format to plain text format for compilation. Plase fornal lines in output files in C++ format (focus on spaces).
