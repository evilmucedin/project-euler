#include "../header.h"

#include "lib/quicksort.h"

class Solution {
public:
    vector<int> sortArray(vector<int>& nums) {

    }
};

int main() {
    Solution sol;
    Timer t("Subarray timer");
    cerr << sol.sortArray({5, 1, 1, 2, 0, 0}) << endl;

    return 0;
}
